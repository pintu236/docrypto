import 'package:flutter/material.dart';

const colorPrimary = Colors.white;
const colorTextPrimary = Colors.black;

const colorBackground = Color.fromARGB(255, 248, 248, 248);
const colorInputField = Color.fromARGB(255, 10, 10, 10);
const colorSeparator = Colors.black;
const colorButton = Colors.black;
const colorStarGradient = Color(0xFFFFC56D);
const colorGrey = Color(0xFF959595);
const colorEndGradient = Color(0xFFFF41BF);
